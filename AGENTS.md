See README.md.

When a new pattern is added under `patterns/` (e.g. `P006-*.md`), also add a row for it in the Pattern Index table in README.md. Keep the table in numeric order.

When a new bundle is added under `bundles/` (e.g. `B001-*.md`), also add a row for it in the Bundle Index table in README.md. Keep the table in numeric order.
